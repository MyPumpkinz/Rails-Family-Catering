<fieldset>  
  	<%= f.label :menu, "Menu" %>  
  	<%= f.collection_select :menu_id, Menu.all, :id, :name, class: 'form-control input-lg'%>  
  	<%= f.label :quantity, "Quantity" %>  
  	<%= f.text_field :quantity %>    
  	<%= f.hidden_field :_destroy, class:'btn btn-primary' %>  
  	<%= link_to "remove", '#', class: "remove_fields" %>  
</fieldset> 